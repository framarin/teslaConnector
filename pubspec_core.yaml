name: tesla_connector_core
description: Tesla OAuth2 authentication service - Core Dart library

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crypto: ^3.0.3
  http: ^1.1.0

dev_dependencies:
  test: ^1.24.0